package DB {
    trait Field {
        has store
        has fetch
    }
}

class Point {
    has x is DB::Field : Number = 0
    has y is DB::Field : Number = 0
    print(self::x.isa(Missing))
}

var p = Point.new()
print(Point::x)

