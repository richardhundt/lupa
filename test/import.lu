import Point, answer, cheese from test.shapes
import Point, answer, cheese from test.shapes in Shapes

import test.shapes
import test.shapes as sh

assert(shapes)
assert(Shapes::answer)
assert(sh::answer)

var p = Point.new
p.move(1, 2)

var q = shapes::Point.new
q.move(1, 2)

print("the answer:", answer)
print("cheese?:", cheese)

class Point3D from shapes::Point {
   static has DEBUG = true

   our MODE = 'funky'

   class Inner {
      method greet() {
         print("Hi globe!", Inner.new, Point3D.new)
      }
   }
}

Point3D::Inner.new.greet
print(Point3D::MODE)

