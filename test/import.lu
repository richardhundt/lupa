import from test.shapes in shapes
import Point, answer, cheese from test.shapes

var p = Point.new
p.move(1, 2)

var q = shapes.Point.new
q.move(1, 2)

print("the answer:", answer)
print("cheese?:", cheese)

class Point3D from shapes.Point {
   print("life, the universe and everything? ${shapes.answer}!")
   static has DEBUG = true
   our MODE = 'funky'
   class Inner {
      method greet() {
         print("Hi globe!", Inner.new, Point3D.new)
      }
   }
}

Point3D.Inner.new.greet
print(Point3D.MODE)

