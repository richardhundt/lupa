class Point {
    has x = 0
    has y = 0
    method move(x, y) {
        self.x = x
        self.y = y
    }
}

class Point3D extends Point {
    has z = 0
    method move(x, y, z) {
        super::move(self, x, y)
        self.z = z
    }
}

var p = Point.new()
for i=1,10000000 {
    p.move(i, i+1)
}
print(p.x, p.y)

